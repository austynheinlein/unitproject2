<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>My Shucking Field Notes</h1>
    <h2>An Oyster Guide to the Puget Sound</h2>
    <nav>
            <ul>

                <li><a href="/users/new">Register</a></li>
                <li><a href="/sessions/new">Log In</a></li>

                <% if(currentUser) { %>
                <li>
                  <form action="/sessions?_method=DELETE" method="POST">
                    <input type="submit" value="Logout"/>
                  </form>
                </li>
                <% } %>

            </ul>
        </nav>
    <hr>
    </br>
    <p>
    <% if(currentUser) { %>
        Welcome <%=currentUser.username%>!
    <% } else { %>
        Welcome!
    <% } %>
  </p>
    <ul>
        <% for(let i = 0; i < oyster.length; i++){ %>
            <li>
                <a href="/oysters/<%=oyster[i].id%>">
                  <%=oyster[i].name%>
                </a>
            </li>
        <% } %>
    </ul>
    <nav>
        <a href="/oysters/new">Add an Oyster Finding!</a>
    </nav>

  </body>
</html>
